{
  "name": "replygenie-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Test suite for ReplyGenie extension logic (pure functions only — no Chrome APIs).",
  "scripts": {
    "test": "npx --no-install jest --runInBand",
    "test:eval": "npx --no-install jest src/tests/eval.test.js --verbose"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "setupFiles": [
      "./src/tests/setup.js"
    ]
  }
}
